

def image_import(filename):
    
    array = f(filename)
    # Check dimensions of array
    # Normalize array using function from import_array
    # Write out csv file: same as filename, but different extension
    return array